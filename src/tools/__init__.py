"""Tool modules. Each module exposes a FastAPI ``router`` and a
``register_mcp_tools(mcp: FastMCP)`` helper so the same logic is reachable
via REST/OpenAPI (Security Copilot) and MCP (VS Code / Claude Desktop).
"""
